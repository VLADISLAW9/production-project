import { createReduxStore } from './config/store';
import { StoreProvider } from './ui/StoreProvider';
import type { StateSchema } from './config/StateSheme';

export {
    StoreProvider,
    createReduxStore,
    StateSchema,
};
